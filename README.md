# MX-GDL-Central-Bookings
A Database created for Central Bookings

To populate the database:
python manage.py makemigrations -> python manage.py migrate -> python populate.py

then run the sever:
python manage.py runserver
